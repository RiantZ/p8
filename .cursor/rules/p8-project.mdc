---
description: p8 project overview — build commands, directory structure, dependencies, key conventions
alwaysApply: true
---

# p8 Project Context

This is Protocol8 (p8) — a C/C++ instrumentation and telemetry library.

For build commands, project structure, dependencies, error handling conventions, and other project-level details, read the `CLAUDE.md` file in the repository root before proceeding.
